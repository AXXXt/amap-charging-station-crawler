# 更新日志

## [未发布] — 2026-08-13

### 本次交付结果

- 基于 `cleaned_stations.csv` 完成真机全量采集：测试表 207 行、194 个唯一高德 POI
- 205/207 行具备详细信息，测试表详细覆盖率达到 **99.03%**
- 192/194 个唯一 POI 达到详细标准，唯一站点详细率达到 **98.97%**
- 最终生成独立 Excel 质量报告，包含覆盖率总览、站点明细、任务汇总、失败项、采集尝试和字段说明
- 剩余 2 个高德侧无法进入详情的 POI 已在报告中保留：`B0LGXSBNFN`、`B0L2ZX4LMS`

### 普查站点即时详情修复

- 搜索结果改为增量处理：发现当前屏幕的第一个完整站点后立即进入详情，详情返回后再继续扫描，避免先滚完整个列表再点击
- 当前可见站点直接使用卡片坐标点击，底部半露卡片延后到下一屏，避免点击导航栏或无效区域
- 中文搜索优先通过聚焦控件 `set_text()` 写入，避免 ADB 键盘路径把“郑州中原区重卡充电站”转换成 `???`
- 扩展紧凑详情页识别，支持“营业中 / 24小时营业 / 详情 / 地图 / 导航 / 路线”结构，并解析 `24小时营业`
- 移除详情采集中的局部 `re` 导入，修复基础详情页完成采集时的 `UnboundLocalError`
- 真机验证“郑州中原区重卡充电站”：首站详情成功，处理首站时滚动次数为 **0**

### 核心稳定性修复

**停止事件** (`crawler.py`, `api_server.py`)
- `AmapCrawler` 持有并统一检查 `stop_event`
- 页面等待、搜索滚动、详情采集和区县循环均可及时响应停止信号
- 启动任务时先设置运行状态，再启动后台线程，消除快速点击启动/停止时的竞态

**递归包装器** (`api_server.py`)
- 搜索进度和采集进度包装器改为每个采集器实例只安装一次
- 避免城市/区县循环中重复包装方法导致递归调用、进度重复累计和调用栈增长

**API 路由** (`api_server.py`)
- 单站点路由改为 `/api/stations/{station_id:int}`
- `/api/stations/nearby` 不再被动态站点 ID 路由提前匹配

**区县过滤** (`crawler.py`)
- 站点详情先完成校验，再写入最终结果
- 移除“连续两条跨区结果即提前结束”的漏站逻辑
- 搜索卡片同步提取可见行政区地址，明确跨区时无需进入详情即可跳过，减少无效点击和等待
- 区县判断改为“目标区 / 明确跨区 / 地址未知”三态；地址未知仍进入详情核验，不计入提前停止阈值
- 允许早期少量跨区结果夹杂；至少命中过一个目标区站点后，连续 4 个明确跨区结果才结束当前区县搜索
- 站名只用于正向确认目标区，不能单独作为跨区证据，避免品牌名或旧站名导致误过滤

### 详情采集准确性

- 新增统一页面状态识别模块 `page_state.py`，区分首页、搜索结果、详情、价格详情、弹窗和未知页
- 优先使用测试表中的高德 POI ID，通过 `amapuri://poi/detail` 精确唤起详情
- 表内经纬度优先作为最终坐标，避免错误地理编码覆盖原始坐标
- 修复搜索结果页被误判为详情页后“随便滚动几次直接返回”的问题
- 支持 POI 地图底卡详情，识别“展开列表 + 暂无更多内容”等结构特征
- 支持“充电站、超充站、快充站、重卡站、充换电站”等名称形式
- 支持基础详情、标准详情、24 小时价格趋势页和点击展开分时电价页
- 补充运营商推断、嵌入式设施标签、地址清洗和“暂无营业时间”解析

### 分时电价结构统一

- `full_trend` 页面不再只读取初始 XML：采集器会在初始页和每次滚动后重新检测趋势图或价格卡片入口
- 有设备、当前价或价格区块但尚未出现入口时，最多继续小幅滚动 3 次，解决趋势图在第二、第三屏才出现时被直接漏过的问题
- 趋势卡片首次出现在页面底部但仍被悬浮导航栏遮挡时不会点击；卡片完全进入安全可见区域后才执行点击
- 价格入口按 UI XML 查找真实可点击父容器，优先选择完整趋势图区块，并排除把停车费旁“查看详情”误判为电价入口的超大共享容器
- 无趋势图站点继续支持点击局部价格卡片进入详情页
- 点击后必须确认页面状态为 `PageKind.PRICE_DETAIL`；从深层价格页返回后若站名暂时不在当前屏，会向上恢复并确认原站点详情
- 价格详情页支持单时段页面、内联费用文本、拆分价格节点和重复时段合并
- 分时价格统一为 `time / total_price / elec_fee / service_fee / tag / source`，不再混用旧字段 `price`
- 合并策略改为价格详情优先，站点页内嵌趋势补齐缺失时段或费用字段；点击失败或解析为空时完整保留趋势数据
- 采集结果新增 `price_schedule_source`、`price_detail_attempted` 和 `price_detail_collected`，便于判断数据来源与点击结果

### 视觉审查接入

- 正常路径优先使用 UI XML，避免每个站点都调用视觉模型
- 仅在 XML 状态未知、页面转场异常、弹窗遮挡或恢复失败时调用视觉审查
- 视觉 API 返回错误时自动回退 XML，不把接口错误误判为正常页面
- 千问视觉模型密钥统一从 `DASHSCOPE_API_KEY` 环境变量读取

### 多设备任务调度

- 新增 `task_queue.py`，使用 SQLite 保存幂等任务、优先级、租约、心跳、重试和设备状态
- 新增 `batch_runner.py`，每台在线 ADB 设备运行独立 worker，可并行领取任务
- 用户回传任务使用高优先级 1000，不中断设备当前操作，并由下一台空闲设备优先领取
- 用户在任务执行期间重复提交同一站点时，记录安全重跑请求
- 支持中断释放、过期租约回收、孤儿租约恢复以及失败站点定向重排队
- 新增队列 API：
  - `POST /api/queue/stations`
  - `GET /api/queue/status`
  - `GET /api/queue/stations/{station_id}`

### 报表与行政区处理

- 新增 `prepare_report_data.py`，将 194 个唯一任务按原始记录展开回 207 行
- 城市和区县改为河南行政区白名单识别，避免把“白霜超市”等普通地址词误判为城市
- POI 元数据缺失时可基于原始坐标执行逆地理编码，补全城市和区县
- 最终 Excel 已完成 7 张预览图的视觉检查
- 工作簿包含 65 个公式，未发现 `#REF!`、`#DIV/0!`、`#VALUE!` 等公式错误

### 安全配置

- 删除代码中的高德 API Key 和 MySQL 连接凭据
- `AMAP_API_KEY`、`DASHSCOPE_API_KEY`、`DB_*`、`DEVICE_SERIAL` 和 `ADB_PATH` 改由环境变量提供
- 新增 `.env.example`，并在 `.gitignore` 中排除本地 `.env` 文件
- 未配置 MySQL 时采集和 SQLite 调度仍可运行，数据库类 API 返回 `503`

### Windows 一键启动修复

- 修复原 `start.bat` 使用 UTF-8 BOM 和 LF 换行时被 `cmd.exe` 拆成乱码命令的问题
- `start.bat` 改为 ASCII + CRLF，可直接双击启动并自动打开控制台
- 启动等待增加 30 秒上限，失败时显示 `logs/api_server_error.log`，不再无限循环
- 删除 `taskkill /F /IM python.exe`，不再误杀系统中其他 Python 程序
- 新增 `run_backend.py` 和 `stop_backend.py`，通过专用 PID 文件只管理本项目后端进程
- 已验证启动后 `/api/tasks/status` 与 `/dashboard` 均返回 HTTP 200，停止后端口和进程均正常释放

### 验证

- `python -m compileall -q .` 编译检查通过
- 页面状态、停止事件、API 路由、递归包装器、区县过滤、视觉回退、任务队列、报表处理、增量详情和分时电价流程共 **72 项自动化测试全部通过**，另有 **2 项 subtests 通过**
- 2026-08-13 真机验证“云快充汽车充电站(易岸达物流园充电站)”：趋势入口首次出现时坐标 `(540,2083)` 被底部操作栏遮挡；第二次滚动后坐标变为 `(540,1650)`，成功进入价格详情并解析 **7 个**完整时段
- 2026-08-13 真机验证“郑州中原区重卡充电站”：共发现 7 个候选，仅进入 2 个中原区站点详情；5 个明确跨区结果被预过滤，连续 4 个跨区尾部后仅滚动 2 次即停止
- 真机补采的 5 个可恢复失败站点全部成功

### 主要新增文件

| 文件 | 用途 |
|------|------|
| `page_state.py` | 统一页面状态识别 |
| `task_queue.py` | SQLite 多设备任务队列 |
| `batch_runner.py` | 多设备 worker 与批量采集入口 |
| `prepare_report_data.py` | 最终报表数据展开和行政区处理 |
| `test_page_state.py` | 页面状态测试 |
| `test_regressions.py` | 核心问题回归测试 |
| `test_visual_check.py` | 视觉审查测试 |
| `test_task_queue.py` | 调度队列测试 |
| `test_prepare_report_data.py` | 报表数据测试 |

---

## [5c75ab1] — 历史版本

- 跨城市采集前搜索 `{city}市` 切换地图中心
- 初步增加地址与目标区县匹配校验
